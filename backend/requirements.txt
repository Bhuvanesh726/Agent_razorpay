fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.13.5
pydantic-settings==2.7.0
python-dotenv==1.2.3
psycopg[binary]==3.2.3
pytest==8.3.4
pytest-asyncio==0.25.2
agno==3.0.5
# agno's NVIDIA model wraps its OpenAI-compatible client, so `openai` is a hard
# runtime import (agno/models/openai/chat.py raises ImportError without it) even
# though this project never calls OpenAI. Was previously only present because a
# transitive install happened to pull it into the local venv — a clean install
# failed at import until it was declared here.
openai==3.7.0
razorpay==2.0.1
# Imported directly by app/payments/gateway.py.
requests==2.34.2
authlib==1.3.2
PyJWT==2.10.1
itsdangerous==2.2.0
# Required by starlette's TestClient, which the whole test suite runs on.
httpx==0.28.1
